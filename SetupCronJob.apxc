// This example is run the batch update class in every 5 mins
for (Integer i = 0; i < 60; i += 5) {
  String cronExp = '0 ' + i + ' * * * ?';
  String jobName = 'evona-send-account-updates-' + i;
  System.schedule(jobName, cronExp, new BatchSendAccountsEvona());
}
